export default function AdminPayments() {
  return (
    <div style={{ padding: 'var(--spacing-xl)' }}>
      <h1 style={{ fontSize: '2rem', marginBottom: '16px' }}>Payments & Subscriptions</h1>
      <p style={{ color: 'var(--color-text-muted)' }}>View billing history, active subscriptions, and connect Stripe / M-Pesa gateways.</p>
    </div>
  );
}
